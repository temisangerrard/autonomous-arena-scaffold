{
  "name": "@arena/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "hardhat node",
    "build": "hardhat compile",
    "test": "hardhat test",
    "typecheck": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:base": "hardhat run scripts/deploy.ts --network base",
    "deploy-pool:local": "hardhat run scripts/deploy-pool.ts --network localhost",
    "deploy-pool:sepolia": "hardhat run scripts/deploy-pool.ts --network sepolia",
    "deploy-pool:base": "hardhat run scripts/deploy-pool.ts --network base",
    "verify:base": "hardhat run scripts/verify.ts --network base",
    "verify:sepolia": "hardhat run scripts/verify.ts --network sepolia"
  },
  "devDependencies": {
    "@fastify/busboy": "^3.2.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.2.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.24.1",
    "ts-node": "^10.9.2",
    "typechain": "^8.3.2",
    "typescript": "^5.7.3",
    "yocto-queue": "^0.1.0"
  }
}
